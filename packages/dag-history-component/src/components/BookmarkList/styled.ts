import * as react from 'react'
import styled, { StyledComponentClass } from 'styled-components'

export const Bookmarks = styled.div`
	display: flex;
	flex-direction: column;
	flex: 1;
	max-height: 100%;
	overflow-y: scroll;
`

export const NoBookmarkText = styled.div`
	font-size: 16pt;
	flex: 1;
`
